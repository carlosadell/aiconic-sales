{
  "name": "aiconic-reachout",
  "version": "1.0.0",
  "private": true,
  "description": "Daily reach-out list for booked leads GoHighLevel could not reach. Live pipeline, grouped by salesperson, with a send-SMS action.",
  "engines": {
    "node": ">=18"
  }
}
